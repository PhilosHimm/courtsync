{
  "name": "@courtsync/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CourtSync domain model: competitions, sessions, participants, matches, payments.",
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./sql/*": "./sql/*"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^3.0.0"
  }
}
