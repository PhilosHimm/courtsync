{
  "name": "@courtsync/scheduling",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Scheduling algorithms: pool play, league fixtures, drop-in rotation, referees, seeding, standings.",
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@courtsync/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^3.0.0"
  }
}
